<Window x:Class="kursOOP.Views.PropertyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление недвижимостью" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="PropertyDataGrid" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Добавить" Margin="5" Click="AddProperty"/>
            <Button Content="Редактировать" Margin="5" Click="EditProperty"/>
            <Button Content="Удалить" Margin="5" Click="DeleteProperty"/>
        </StackPanel>
    </Grid>
</Window>
